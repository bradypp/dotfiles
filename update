#!/usr/bin/bash
# Refresh system-package and user-managed software inventories.
set -euo pipefail
repo=${DOTFILES_REPO:-$(cd -- "${BASH_SOURCE[0]%/*}" && pwd -P)}
"$repo/packages/update" "$@"
"$repo/packages/update-user"
