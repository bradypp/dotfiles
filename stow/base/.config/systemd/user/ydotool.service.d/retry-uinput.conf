[Unit]
# The user manager can start before logind grants the active seat access to
# /dev/uinput. Keep retrying instead of exhausting the default start limit.
StartLimitIntervalSec=0

[Service]
RestartSec=2
