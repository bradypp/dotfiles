#!/usr/bin/bash
# Install missing tools declared in the Stowed mise configuration.
set -euo pipefail

command -v mise >/dev/null 2>&1 || {
    printf '%s\n' 'Missing command: mise' >&2
    exit 1
}
mise install
